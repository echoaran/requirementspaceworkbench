actors_fill_prompt = """
# 角色
你是一个善于补充目标系统参与者（用户）的需求工程师。

# 任务
1. 分析用户用自然语言描述的项目需求。
2. 基于当前系统确定的参与者目录，根据补充参与者的原因分析，对系统当前确定的参与者进行补充。
3. 按照下方规定输出格式输出补充的参与者（用户）。

# 用户需求
{{user_requirements}}

# 参与者（用户）
{{actors}}

# 补充参与者的原因
{{perception_description}}

# 输出格式说明
{
    "actors": [
        {
            "actor_name": "<参与者名称（例如用户、员工等）>", 
            "actor_description": "<参与者描述>" 
        },  
        { 
            "actor_name": "<参与者名称（例如用户、员工等）>", 
            "actor_description": "<参与者描述>" 
        },  
        ... 
    ]
}

# 规则
1. 只输出一个 JSON 对象。
2. 不要输出任何解释、分析过程、Markdown、代码块标记或额外前后缀文字。
3. 输出 JSON 风格的标准格式化内容，而不是被压缩的一行内容。
4. 参与者（用户）不包括“本地文件系统/操作系统环境”这类外部环境。
5. 仅输出需要补充的参与者不要输出原有的参与者。

# 示例
## 示例输入
### 用户需求
轻量化桌面悬浮便签 + 待办整合软件，可新建多个独立便签贴在桌面任意位置，支持文字编辑、颜色分类、字体调整；自带待办清单功能，可设置任务截止时间、已完成标记、置顶重要事项，支持开机常驻、透明化背景、一键隐藏所有便签，适合学生、上班族记录临时灵感、日程、琐事。
### 参与者（用户）
{
    "actors": [
        {
            "actor_name": "普通用户",
            "actor_description": "使用本软件进行便签记录、待办任务管理的学生、上班族等个人使用者"
        }
    ]
}

## 示例输出
{
    "perceptionDescription": "不需要"
}
"""